- @users.each do |user|
    .col-md-4.col-xs-12
        .media.my-3.rounded.bg-white
            - if user.image?
                = image_tag user.image.url,class: 'm-4',size: '100x100'
            - else
                = image_tag "/assets/default.jpg",class: 'm-4',size: '100x100'
            .media-body
                .h5 
                    = link_to user.name, user,class: 'mt-2 mb-1 badge badge-pill badge-primary'
                .h6
                    = user.sex
                .h6
                    | キルレ：
                    = user.strength
                .h8.mr-2 
                    |登録日：
                    = I18n.l(user.updated_at, format: :long)